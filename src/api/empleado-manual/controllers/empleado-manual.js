'use strict';

/**
 * empleado-manual controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::empleado-manual.empleado-manual');
